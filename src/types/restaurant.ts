export interface Restaurant {
  id: number;
  name: string;
  rating: number;
  location: string;
  cuisine: string;
  description: string;
  priceRange: string;
  reviews: string;
  image: string;
}
